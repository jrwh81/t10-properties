#!/usr/bin/env bash
set -euo pipefail
cd "$(dirname "$0")/.."

echo "== Installing gems =="
bundle install

echo "== Preparing database =="
bin/rails db:prepare

echo "== Ready. Start the server with: bin/rails server =="
