VITE_API_BASE_URL=http://localhost:3000/api/v1
# Used to build absolute og:image/og:url tags in index.html for link
# previews (Slack, iMessage, Twitter, etc). Update to your real domain
# before deploying -- social platforms can't fetch a localhost image.
VITE_SITE_URL=http://localhost:5173
